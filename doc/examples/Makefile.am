EXTRA = cluster-client.vol cluster-server1.vol cluster-server2.vol cluster-server3.vol standalone-client.vol standalone-server.vol
EXTRA_DIST = $(EXTRA)

docdir = $(prefix)/share/doc/$(PACKAGE_NAME)
Examplesdir = $(docdir)/examples
Examples_DATA = $(EXTRA)
